export default class Group {
  constructor(public readonly id: number, public name: string) { }
}

export const defaultGroups = {
  user: "user",
  admin: "admin"
}

const manejaUsuario = () => { // detalle de implementacion
  // algo
}
